# ktmm (Keep That Mouse Moving!) - Java Version

This works the same as the [Python version](https://github.com/ao/ktmm), but written in Java so that it's cross-platform.

## How to run

### Build the app
`sh package.sh`

### Run the app
`java -jar target/ktmm-java-1.0-SNAPSHOT.jar`

